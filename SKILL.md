---
name: daily-task-board-maintenance
description: 维护有既定书写风格的进度表/计划表/看板（HTML 或 Markdown），外加「每日对账桥」——改表前把产出文档里的声称拿到仓库实况（提交/合并事件）校验，再把校验结果落表。三层真相链：代码=真相，文档=声称，表格=安排。触发词：改计划表、进度表、看板、对账、状态核对、更新计划表、表格式回炉。
version: 3.0.0
evidence_status: 源自一个多 agent 项目周计划表的六轮回炉与每日对账实战（30+ 次返工教训）。方法通用；文中具体数值是团队约定，可替换。
agent_created: true
---

# 有既定风格表格的维护 + 每日对账

**适用**：一张已存在、有既定书写风格的表格（周计划、看板、清单），要往里填新行/更新旧行，且读者会逐列检查风格一致性。

**两个核心难点**：
1. **风格是隐式的**，藏在既有行里——按「我觉得清楚」写，会被逐轮指出不一致。
2. **表里的状态容易抄自文档**——文档不可全信，只有仓库事件（提交/合并）是真相。

---

## 一、每日对账桥（改表前先做）

**三层真相链：代码 = 真相；文档 = 声称（不能全信）；表格 = 安排，只落经校验的实况。**
改表不是「读文档抄状态」，是「拿文档当线索 → 去仓库校验 → 把校验结果落表」。

### 第 0 步 · 找全状态副本

同一任务的状态常有 3–4 份副本（任务登记文件、账本、总表、本表），可能互相矛盾。动手前先找齐：

- 命名/编号往往有多套（模块号、线号、任务 ID、代号）——**只搜一套得 0 命中，不能下「不存在」的结论**；
- **读前查文件 mtime**——旧快照会给出与现状相反的结论；
- **工作树 ≠ 主仓**——分支上的工作主仓里看不到。

### 第 1 步 · 对账扫描（三类必查，全部机械可查）

| 声称 | 去仓库查什么 |
|---|---|
| 已完成 / 已合并 | 合并事件**真的存在吗**（PR 状态、merge commit）——「已合入仍写待办」是最高发失真 |
| 可开工 / ready | 声称的前置**真的解除了吗**？引用的基线落后当前 HEAD 多少——落后太多的「可开工」不可信 |
| 阻塞 / blocked | 阻塞理由**被后续进展证伪了吗**——被证伪的阻塞让可做的事白等 |

### 第 2 步 · 对账发现矛盾后

1. **唯一真值源**：裁决一份为权威（通常是任务登记的机器可读 status 字段），其余副本改为**引用**、不再各写一份——复制出去的状态必然失真。
2. **留证据**：矛盾记录在案（偏离记录 / 表内事实行），不静默抹掉。
3. **旧世代归档**：不再执行的任务移入归档区（带时间与理由），退出「待办」判定。
4. **归因**：分清「**漏更新**」与「**回写为零**」——前者修条目即可；后者必须立「交付同窗口回写」的制度，否则每轮复发。

---

## 二、表格本体维护（四步）

### Step 1 · 定基准时间点

从「稳定期」的行提取模板，不从近期行提取——近期行经多轮修补已走样。
验证基准选对没有：基准区间的行在某列的取值应收敛；混着多种写法 = 基准选错了。

### Step 2 · 逐列导出模板

**按列，不按表。** 对每列导出基准区间的原始写法（含样式与符号），写成模板：

- 同一概念在不同表、不同列的写法可以不同——把 A 表的写法套到 B 表就是错；
- **符号（✅、emoji、色块）也是列模板的一部分**：该列既有行没有的符号，不要为了「更清楚」而引入；
- 注意标点与空白：分隔符前后有没有空格、是 `/` 还是 ` / `、连字符是哪种（`-` vs `−` U+2212）。

### Step 3 · 按模板全表改

**全表扫描「名义状态 vs 实际状态」，不是只改自己刚动过的行**：

- 已完成但状态仍写计划的行；
- 引用过期措辞的行（「本地待提交」但早已提交）；
- 已完成行里出现未来时间词（今晚/明日）→ 改过去式或相对词；
- 分区与状态不自洽的行（已完成的行躺在「排队」区）。

长文件多点改动**用脚本串行替换**：每次替换前 assert 锚点存在且唯一；删除+插入混做时**一步一校验**。

### Step 4 · 结构校验

每次改完必跑：

- 标签配对（开=闭：table/tr/td/div 等）；
- 节点/锚点 id 无重复；
- 关键短语计数（追加式改动易产生重复行）；
- 本表禁止项归零（历史上被纠正过的符号/措辞）。

---

## 三、事实源纪律

| 判据 | 权威来源 |
|---|---|
| 某任务是否完成 | 任务登记的状态字段 **+ 仓库事件**（提交/合并）——规划文档写的是**意图**，不是状态 |
| 落位日期 | 仓库事件的**真实时间**（注意时区转换），不是计划日期 |
| 谁交付的 | **提交作者**（git author），不凭线索/归属名猜测 |
| 找不到记录 ≠ 没做 | 大概率改名、换归属、或在未合并分支上——先查开放 PR / 分支 diff，再下结论 |

---

## 四、行样式与状态语义

- **完成态与计划态在视觉上必须可区分**（色条/高亮/tag）。判据是「**状态**」，不是「归属」——禁止给未完成的行上完成样式，那会把「还没做」显示成「已交付」。
- 时间措辞用相对词（当晚 / 次日 / 已合并），不写具体钟点；已完成行不得出现未来时间词。
- 表是**任务台账**：不放机制解释、不放免责加注；只放当日真实发生的事实。

---

## 五、排期与负载（具体数值是团队约定，可替换）

- 团队表中**每个角色的每个周期都应有行**（确无任务写 0）——这是团队进度表，不是负责人工作日志。
- **编排者不能被排成纯验收员**：验收类任务占比设上限（如 ≤50%），且每周期必须有自有任务——否则「只有编排者能做的事」停摆。
- **并行判据 = 文件面零交集**，必须实测（列文件、数出现次数），不按名字猜；全局共享文件归一人。
- **当日交付当日验收**，不隔夜——压缩总工期主要靠这个，不靠加班。
- 非工作日不排任务，但**照实补录**自发提交（零提交写零提交）。
- 工作量分布均匀（逐日差异设上限）；「每天至少 N 条」是下限不是上限，但**不凑数**——rebase、补流程、等审查不是交付，没有新增可验收能力的 PR 不算任务。

---

## 六、反模式（每条一行）

1. **局部修补**：只改刚动过的行 → 必须全表扫描。
2. **按表对齐**：把 A 表写法套到 B 表 → 按列对齐。
3. **无基准提取**：把走样的近期写法当规范 → 先定基准并验证收敛。
4. **自作主张加符号**：该列无先例的 ✅/emoji → 撤回。
5. **过期措辞/未来时间词**留在已完成的行 → 同步改。
6. **拿规划当事实**：规划是意图；登记状态 + 仓库事件才是事实。
7. **按计划日期落位**：按仓库事件真实时间落位（先转时区）。
8. **只看主分支断言「未做」**：工作可能全在未合并分支——先查开放 PR 与分支 diff。
9. **只看近期窗口的提交记录** → 漏记成员提交：全量按天汇总，与表中逐行比对。
10. **凭线索名猜交付者**：以提交作者为准。
11. **「审查」与「合入」混写**：先查有无审查记录，措辞分开（未审的不能写成「已合入」）。
12. **整文件重写长内容**：超长截断失败 → 脚本串行替换。
13. **不做结构校验**：块移动/删除后标签配对与 id 唯一性可能已坏而不自知。
14. **ID 池按脚本重置** → 多轮脚本撞号：全局单调分配，每轮查重。
15. **插入锚点带闭合标签** → 重复闭合：用行首锚点或偏移量。
16. **区块替换未删旧块** → 重复内容：先删后插，立刻数块数。
17. **在文件结尾之外追加内容** → 不渲染：追加后检查尾部是否干净。
18. **「能跑」当「能交付」**：两个里程碑状态必须区分，差距显式写出来。

---

## 与其他技能的边界

- 本 skill 管到「**表上排了什么、依据什么排**」；任务卡怎么发、成员怎么接是**派发协议**的事（不同团队拆法不同）。
- 代码本身的审查用 code review 类技能；本表只登记审查动作与结果。
